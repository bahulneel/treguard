(ns treguard.algo)

;; In a distributed system
;; we can define a process
;; as a series of
;; state transitions.
;; These transitions form a
;; State Trasition System (STS).
;;
;; An STS is similar to a
;; state machine except that it can have
;; an infinite number of states
